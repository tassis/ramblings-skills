export const investigate = {
  description: "Understand an existing system or flow before deciding next action",
  template: "Use ramblings-investigation. Focus on current-state understanding, relevant files, execution flow, assumptions, and likely next steps. Do not jump into fixing before the facts are established. After the investigation, route explicitly to answer-only, debugging, brief writing, planning, or implementation if the current mode allows it."
}
