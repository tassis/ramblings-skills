export const careful = {
  description: "Switch into a more conservative, high-risk workflow posture",
  template: "Treat this task as high-risk enough that caution matters more than speed. Prefer understanding before editing, smaller changes, frequent verification, explicit uncertainty reporting, and scope restraint. Use careful as a command-first posture modifier that should steer the work into the appropriate main workflow rather than acting like its own standalone phase."
}
