export const startFeature = {
  description: "Start a substantial feature workflow from discussion through execution",
  template: "Treat this as the command-first full lifecycle entrypoint for substantial feature, subsystem, or project work. Start with discussion and requirement shaping, then brief-writing by default once direction stabilizes. Escalate to writing-plans only when the user explicitly asks for a plan or when the work is concrete enough to name files, ordered steps, and verification with little ambiguity. Use narrower ramblings skills once the work enters a specific phase. Do not jump into implementation before the shape of the work is clear."
}
