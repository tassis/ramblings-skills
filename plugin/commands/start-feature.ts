export const startFeature = {
  description: "Start a substantial feature workflow from discussion through execution",
  template: "Use ramblings-workflow as backing guidance. Treat this as a command-first full lifecycle entrypoint for substantial feature, subsystem, or project work. Start with discussion and requirement shaping, move to spec-writing once the direction stabilizes, then writing-plans, and only then proceed to execution when the work is explicit enough. Do not jump into implementation before the shape of the work is clear."
}
