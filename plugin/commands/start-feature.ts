export const startFeature = {
  description: "Start a substantial feature workflow from discussion through execution",
  template: "Treat this as the command-first full lifecycle entrypoint for substantial feature, subsystem, or project work. Start with discussion and requirement shaping, move to brief-writing once the direction stabilizes, then writing-plans, and only then proceed to execution when the work is explicit enough. Use narrower ramblings skills once the work enters a specific phase. Do not jump into implementation before the shape of the work is clear."
}
