export const challengeMe = {
  description: "Challenge the current idea, brief, plan, or change from multiple review perspectives",
  template: "Use the Reviewer agent (`@reviewer`) with ramblings-challenge-me. Run a focused review panel around the current artifact, spawn one independent reviewer invocation per selected review lens, preserve reviewer positions, surface meaningful disagreement, and synthesize only after the reviewer stances are explicit."
}
