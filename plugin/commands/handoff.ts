export const handoff = {
  description: "Create a dated handoff artifact for a later session or continuation",
  template: "Use ramblings-handoff. Create a compact dated handoff artifact under .ramblings/handoffs/ only if this work is truly being handed off. Keep handoffs append-only; do not overwrite a single current handoff file. Include compact frontmatter metadata with: topic, work_unit, references, optional supersedes, and status. Reference existing briefs, plans, reviews, and debug notes instead of duplicating them. Include current objective, current state, blockers, suggested next step, and suggested next skills."
}
