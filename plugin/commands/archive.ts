export const archive = {
  description: "Archive completed work or consolidate overlapping active artifacts",
  template: "Use ramblings-archive. Handle explicit operator-driven cleanup for completed work units, canonical artifact consolidation, and safe archive packaging. Prefer a single clean canonical outcome, preserve historical context, and stop when multiple candidates or source-of-truth ambiguity remain. Keep the archive action explicit and avoid broad active-work execution."
}
